spa@node1:~$ sudo crontab -l -u fengwei
* /var/tmp/.cache/upd >/dev/null 2>&1
  spa@node1:~$ sudo cat /etc/passwd | cut -d: -f1 | xargs -I {} sudo crontab -u {} -l 2>/dev/null
* /tmp/.bash/.bash
* /tmp/.py/.python
* /var/tmp/.cache/upd >/dev/null 2>&1

```
sudo cat /etc/passwd | cut -d: -f1 | xargs -I {} sudo crontab -u {} -l 2>/dev/null
sudo ls -l /proc/847787/exe
```

如何处理？

- [ ] 处理恶意程序（1.删除恶意文件2.删除crontab）
- [ ] 改ssh为强密码



服务器系统整理和资源分配

